{
  "name": "vite-vanilla-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "sass": "^1.81.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "firebase": "^11.0.2"
  }
}
